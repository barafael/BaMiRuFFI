[package]
name = "BaMiRuFFI"
version = "0.1.0"
build = "build.rs"
authors = ["Rafael Bachmann <triolenzunge@gmail.com>"]

[build-dependencies]
bindgen = "0.59.2"
cc = "1.0.72"

[dependencies]
libc = "0.2.112"

# LeakSanitizer doesn't work reliably with opt-level=0
[profile.dev]
opt-level = 1
